<style>
  .btn-search {
    background-color: #fe424d;
    color: #fff;
    border-radius: 25px;
    padding: 0 0.8rem;
    font-size: 0.9rem;
  }
  .btn-search:hover {
    background-color: #fe424d;
    color: #fff;
  }
  .btn-search i {
    margin-right: 0.3rem;
  }
  .search-input {
    border-radius: 25px;
    padding: 0.4rem 1rem;
    font-size: 0.9rem;
  }

  /* Mobile adjustments */
  @media (max-width: 768px) {
    .navbar {
      padding: 0.3rem 0.6rem;
    }
    .navbar-brand i {
      font-size: 1.2rem; /* Smaller logo */
      margin-right: 0.4rem; /* Less gap */
    }
    .btn-search {
      font-size: 0.75rem;
      padding: 0.2rem 0.5rem;
    }
    .search-input {
      font-size: 0.75rem;
      padding: 0.3rem 0.6rem;
    }
    .navbar-toggler {
      font-size: 0.75rem; /* Smaller = button */
      padding: 0.2rem 0.4rem;
      margin-left: 0.4rem; /* Less gap */
    }
    .navbar-center {
      flex-grow: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 0.4rem; /* Reduce space */
    }
  }

  /* White background when menu expands */
  .navbar-collapse.show {
    background: white;
    padding: 1rem;
    border-radius: 10px;
    text-align: center;
  }
</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid d-flex align-items-center">
    <!-- Logo -->
    <a class="navbar-brand" href="/listings">
      <i class="fa-solid fa-building-user"></i>
    </a>

    <!-- Search Bar (Visible on mobile & PC) -->
    <div class="navbar-center">
      <form class="d-flex" role="search">
        <input class="form-control search-input" type="search" placeholder="Search for locality, landmarks, project, or bouilders" aria-label="Search">
        <button class="btn btn-search" type="submit">
          <i class="fa-solid fa-magnifying-glass"></i>Search
        </button>
      </form>
    </div>

    <!-- Navbar Toggle Button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
        <a class="nav-link" href="/listings/new">Sell Your land & Home</a>
<a class="nav-link" href="/listings/auction/live">Live Auction</a>
      </div>

      <!-- Authentication Links -->
      <div class="navbar-nav ms-auto">
        <% if (!currentUser) { %>
          <a class="nav-link" href="/signup"><b>Sign up</b></a>
          <a class="nav-link" href="/login"><b>Log in</b></a>
        <% } else { %>
          <a class="nav-link" href="/logout"><b>Log out</b></a>
        <% } %>
      </div>
    </div>
  </div>
</nav>